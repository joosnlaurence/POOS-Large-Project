export type Fountain = {
    id: string;
    name: string;
    fountainLocation: [number, number];
    fountainDescription: string;
    filterStatus: string;
    floor: string;
    imageUrl: string;
}